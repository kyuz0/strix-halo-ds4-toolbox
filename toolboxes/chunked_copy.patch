diff --git a/ds4_cuda.cu b/ds4_cuda.cu
index aaaaaaa..bbbbbbb 100644
--- a/ds4_cuda.cu
+++ b/ds4_cuda.cu
@@ -1618,6 +1618,11 @@ extern "C" int ds4_gpu_set_model_map(const void *model_map, uint64_t model_size)
         }
     }
 
+    const char *chunked_env = getenv("DS4_CUDA_COPY_MODEL_CHUNKED");
+    if (chunked_env && chunked_env[0]) {
+        return 1;
+    }
+
     unsigned int flags = cudaHostRegisterMapped | cudaHostRegisterReadOnly;
     if (getenv("DS4_CUDA_HOST_REGISTER_PLAIN") != NULL) {
         flags = cudaHostRegisterMapped;
